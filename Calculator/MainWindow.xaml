<Window x:Class="Calculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Calculator" Height="350" Width="300">
    <Grid>
        <TextBox x:Name="InputBox" HorizontalAlignment="Stretch" VerticalAlignment="Top" Margin="10" FontSize="24" TextAlignment="Right"/>
        <Grid Margin="10,60,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Content="1" Grid.Row="3" Grid.Column="0" FontSize="20" Click="Number_Click"/>
            <Button Content="+" Grid.Row="1" Grid.Column="3" FontSize="20" Click="Operation_Click"/>
            <Button Content="=" Grid.Row="4" Grid.Column="2" FontSize="20" Click="Equal_Click"/>
            <Button Content="C" Grid.Row="4" Grid.Column="0" FontSize="20" Click="Clear_Click"/>
        </Grid>
    </Grid>
</Window>